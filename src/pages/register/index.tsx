import MainTemplate from "../../templates/main";

const RegisterPage: React.FC = () => {
    return (
        <MainTemplate>
            
        </MainTemplate>
    );
}

export default RegisterPage;